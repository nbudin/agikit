if (f5) {
  reset(f100);
  reset(f101);
  reset(f107);
  reset(f112);
  reset(f111);
  reset(f118);

  if (v1 == 173) {
    position(o0, 50, 165);
  }

  if (v1 == 189) {
    ignore.blocks(o0);
    position(o0, 81, 77);
    set.loop(o0, 2);
    set.cel(o0, 0);
    v6 = 0;
    set(f107);
    program.control();
  }
  load.pic(v0);
  draw.pic(v0);
  discard.pic(v0);
  set.horizon(0);
  animate.obj(o1);
  load.view(50);
  set.view(o1, 50);
  set.loop(o1, 0);

  if (f107) {
    set.cel(o1, 2);
  } else {
    set.cel(o1, 0);
  }
  ignore.blocks(o1);
  ignore.objs(o1);
  position(o1, 75, 79);
  set.priority(o1, 4);
  stop.cycling(o1);
  draw(o1);
  show.pic();
  draw(o0);
}

if (f107) {
  move.obj(o0, 81, 87, 1, f112);
}

if (f112) {
  load.sound(4);
  sound(4, f153);
  reverse.loop(o1, f118);
  reset(f112);
  reset(f107);
  observe.blocks(o0);
  observe.objs(o0);
  player.control();
}

if (said("examine") || said("examine", "a") || said("examine", "room")) {
  print("You're at the front entrance of the Fine Arts building.");
}

if (said("examine", "arts") || said("examine", "building")) {
  print("It's John's home away from home.");
}

if (said("examine", "floor")) {
  print("You see nothing important.");
}

if (said("examine", "sidewalk")) {
  print("It's just a sidewalk.");
}

if (said("examine", "wall")) {
  print("There's nothing special about it.");
}

if (said("examine", "window")) {
  print("You can see inside the building.");
}

if (said("examine", "tree")) {
  print("There's a tree here. Wowee!");
}

if (said("o", "arts", "d") || said("o", "t", "d") || said("o", "d") || said("o", "building", "d")) {
  if (posn(o0, 74, 82, 93, 86)) {
    program.control();
    load.sound(3);
    sound(3, f153);
    end.of.loop(o1, f101);
  } else {
    print("You're not close enough.");
  }
}

if (f101) {
  move.obj(o0, 77, 83, 1, f100);
}

if (f100) {
  reset(f101);
  ignore.blocks(o0);
  ignore.objs(o0);
  move.obj(o0, 77, 75, 1, f111);
}

if (f3) {
  erase(o1);
  reset(f100);
  reset(f101);
  reset(f107);
  reset(f112);
  reset(f111);
  reset(f118);
  new.room(189);
}

if (v2 == 3) {
  erase(o1);
  reset(f100);
  reset(f101);
  reset(f107);
  reset(f112);
  reset(f111);
  reset(f118);
  new.room(173);
}
return();


// messages
#message 1 "You're at the front entrance of the Fine Arts building."
#message 2 "It's John's home away from home."
#message 3 "You see nothing important."
#message 4 "It's just a sidewalk."
#message 5 "There's nothing special about it."
#message 6 "You can see inside the building."
#message 7 "There's a tree here. Wowee!"
#message 50 "You're not close enough."
