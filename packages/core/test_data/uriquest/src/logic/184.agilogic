if (f5) {
  load.pic(v0);
  draw.pic(v0);
  discard.pic(v0);
  set.horizon(0);
  reset(f103);

  if (v1 == 172) {
    position(o0, 130, 165);
  }
  reset(f100);
  reset(f101);
  reset(f102);
  reset(f111);
  reset(f110);
  reset(f112);
  animate.obj(o1);
  load.view(48);
  set.view(o1, 48);
  set.loop(o1, 0);
  set.cel(o1, 0);
  set.priority(o1, 10);
  v55 = 1;
  cycle.time(o1, v55);
  position(o1, 0, 100);
  start.cycling(o1);
  animate.obj(o2);
  load.view(49);
  set.view(o2, 49);
  set.loop(o1, 0);
  set.cel(o1, 0);
  set.priority(o2, 9);
  position(o2, 70, 100);

  if (!has(i32)) {
    animate.obj(o3);
    load.view(75);
    set.view(o3, 75);
    position(o3, 95, 100);
    set.priority(o3, 4);
    ignore.blocks(o3);
    ignore.objs(o3);
    stop.cycling(o3);
    draw(o3);
  }
  draw(o0);
  show.pic();
}

if (said("examine", "floor") || said("examine", "road")) {
  if (has(i32)) {
    print("There's nothing there.");
  } else {
    set(f110);
    print("Well, well, well. You spy a nice shiny quarter lying in the street!");
  }
}

if (said("use", "fish hook") || said("use", "fish hook", "coin") || said("use", "fish hook", "acquire", "coin") || said("use", "fish hook", "reach", "coin") || said("acquire", "coin", "fish hook") || said("use", "fish hook", "road") || said("cast", "fish hook") || said("cast", "fish hook", "road")) {
  if (f110) {
    if (v50 == 3) {
      if (has(i32)) {
        print("Huh?");
      } else {
        if (has(i33)) {
          ignore.blocks(o0);
          ignore.objs(o0);
          program.control();
          set(f112);
        } else {
          print("What??");
        }
      }
    } else {
      print("Huh?");
    }
  } else {
    print("Huh?");
  }
}

if (f112) {
  move.obj(o0, 95, 115, 1, f111);
}

if (f111) {
  reset(f112);
  reset(f111);
  observe.blocks(o0);
  observe.objs(o0);
  player.control();
  get(i32);
  drop(i33);
  erase(o3);
  v3 += 1;
  load.sound(12);
  sound(12, f153);
  print("Using your makeshift fishing line and hook, you snag the elusive coin from the perilous street. Nice job!!!");
  print("Since you don't need the fishing line anymore, you toss it.");
}

if (said("examine") || said("examine", "a") || said("examine", "room")) {
  print("You're in the southeastern part of campus on Upper College Road.");
}

if (said("examine", "building")) {
  print("Just frat houses, nothing more.");
}

if (said("examine", "sidewalk")) {
  print("It's just a sidewalk.");
}

if (said("cross", "road")) {
  print("Just try it... I dare ya.");
}

if (said("examine", "tree")) {
  print("The trees are of no concern.");
}

if (said("examine", "bush")) {
  print("The bushes are of no concern.");
}

if (said("examine", "fence")) {
  print("A poor attempt to keep drunk frat boys from stumbling into the street.");
}

if (said("chat")) {
  print("You should stop talking to yourself.");
}

if (said("acquire", "coin")) {
  if (has(i32)) {
    print("There is no quarter, as Robert Plant once said.");
  } else {
    print("You can't reach it.");
  }
}

if (f3) {
  program.control();
  ignore.objs(o0);
  ignore.objs(o1);
  move.obj(o0, 95, 100, 1, f100);
}

if (f100) {
  set.loop(o0, 1);
  set.cel(o0, 0);
  load.sound(18);
  sound(18, f153);
  draw(o1);
  move.obj(o1, 45, 100, 1, f101);
  reset(f100);
}

if (f101) {
  erase(o0);
  draw(o2);
  move.obj(o1, 150, 100, 1, f102);
  reset(f101);
}

if (f102) {
  reset(f102);
  erase(o1);
  v35 = 3;
}

if (v2 == 2) {
  if (!f103) {
    print("You don't need to go that way.  This game is big enough as it is.");
    set(f103);
  }
}

Address541:

if (v2 == 4) {
  reset(f103);
  erase(o1);
  erase(o2);
  new.room(183);
}

if (v2 == 3) {
  reset(f103);
  erase(o1);
  erase(o2);
  new.room(172);
}
return();


// messages
#message 1 "There's nothing there."
#message 2 "Well, well, well. You spy a nice shiny quarter lying in the street!"
#message 3 "Huh?"
#message 4 "What??"
#message 5 "Using your makeshift fishing line and hook, you snag the elusive coin from the perilous street. Nice job!!!"
#message 6 "Since you don't need the fishing line anymore, you toss it."
#message 7 "You're in the southeastern part of campus on Upper College Road."
#message 8 "Just frat houses, nothing more."
#message 9 "It's just a sidewalk."
#message 10 "Just try it... I dare ya."
#message 11 "The trees are of no concern."
#message 12 "The bushes are of no concern."
#message 13 "A poor attempt to keep drunk frat boys from stumbling into the street."
#message 14 "You should stop talking to yourself."
#message 15 "There is no quarter, as Robert Plant once said."
#message 16 "You can't reach it."
#message 17 "You don't need to go that way.  This game is big enough as it is."
