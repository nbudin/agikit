if (f5) {
  load.pic(v0);
  draw.pic(v0);
  discard.pic(v0);
  set.horizon(0);

  if (v1 == 194) {
    position(o0, 10, 150);
    v6 = 1;
  }

  if (v1 == 196) {
    position(o0, 60, 95);
    v6 = 1;
  }
  reset(f101);
  reset(f102);
  v51 = 195;

  if (f160) {
    animate.obj(o1);
    load.view(61);
    set.view(o1, 61);
    set.loop(o1, 0);
    set.cel(o1, 0);
    position(o1, 15, 133);
    start.cycling(o1);
    draw(o1);
  }
  prevent.input();
  draw(o0);
  show.pic();

  if (f160) {
    load.sound(2);
    sound(2, f153);
    print("Oh, shit!!!  It's the psycho janitor of Lippitt Hall who almost killed Ed's roommate!! You'd better get the hell away from him and into your cubicle where it's safe!!!");
    set(f102);
    load.sound(11);
    set(f108);
  }
}

Address99:

if ((f160) && (f102)) {
  get.posn(o0, v100, v101);
  v104 = v100;
  v106 = v100;
  v105 = v101;
  v107 = v101;
  v104 -= 10;
  v105 -= 5;
  v106 += 17;
  v107 += 6;
  get.posn(o1, v102, v103);

  if (((v102 > v104)) && ((v102 < v106)) && ((v103 > v105)) && ((v103 < v107))) {
    v35 = 4;
    stop.motion(o1);
    stop.motion(o0);
    stop.cycling(o0);
  }
}

Address176:

if ((f160) && (f102)) {
  if (!f101) {
    follow.ego(o1, 0, f101);
  }

  if (f108) {
    sound(11, f108);
  }
}

Address208:

if (v2 == 3) {
  accept.input();
  new.room(194);
}

if (f3) {
  if (f160) {
    v3 += 1;
    load.sound(1);
    sound(1, f153);
    print("Congratulations.  By deftly evading the evil psycho janitor, you have saved yourself from almost certain doom. Nice job!");
    reset(f160);
    reset(f101);
    reset(f102);
    reset(f108);
    erase(o1);
    accept.input();
    new.room(196);
  } else {
    reset(f101);
    reset(f102);
    reset(f108);
    erase(o1);
    accept.input();
    new.room(196);
  }
}
return();


// messages
#message 1 "Oh, shit!!!  It's the psycho janitor of Lippitt Hall who almost killed Ed's roommate!! You'd better get the hell away from him and into your cubicle where it's safe!!!"
#message 2 "Congratulations.  By deftly evading the evil psycho janitor, you have saved yourself from almost certain doom. Nice job!"
#message 50 "You're not close enough."
