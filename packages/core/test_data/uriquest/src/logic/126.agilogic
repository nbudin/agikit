if (f5) {
  reset(f100);
  reset(f101);
  reset(f107);
  reset(f112);
  reset(f111);
  reset(f118);

  if (v1 == 125) {
    ignore.blocks(o0);
    position(o0, 47, 104);
    set.loop(o0, 2);
    set.cel(o0, 0);
    v6 = 0;
    set(f107);
    program.control();
  }
  load.pic(v0);
  draw.pic(v0);
  discard.pic(v0);
  set.horizon(0);
  animate.obj(o1);
  load.view(47);
  set.view(o1, 47);
  set.loop(o1, 0);

  if (f107) {
    set.cel(o1, 3);
  } else {
    set.cel(o1, 0);
  }
  ignore.blocks(o1);
  ignore.objs(o1);
  position(o1, 43, 107);
  set.priority(o1, 4);
  stop.cycling(o1);
  draw(o1);
  show.pic();
  draw(o0);
}

if (f107) {
  move.obj(o0, 55, 104, 1, f112);
}

if (f112) {
  load.sound(4);
  sound(4, f153);
  reverse.loop(o1, f118);
  reset(f112);
  reset(f107);
  observe.blocks(o0);
  observe.objs(o0);
  player.control();
}

if (said("examine") || said("examine", "a") || said("examine", "room")) {
  print("You're in front of Ballantine Hall.");
}

if (said("examine", "ballantine") || said("examine", "building")) {
  print("Yup. Ballantine, like the beer.");
}

if (said("examine", "floor")) {
  print("You see nothing important.");
}

if (said("examine", "bush")) {
  print("Na' ga' do it!.");
}

if (said("examine", "tree")) {
  print("Yes, there is a tree here.");
}

if (said("examine", "path")) {
  print("It leads to the door.");
}

if (said("examine", "sidewalk")) {
  print("It leads to the door.");
}

if (said("examine", "window")) {
  print("You see nothing.");
}

if (said("examine", "rail")) {
  print("Students usually sit on it. Now that doesn't mean you should try it.");
}

if (said("sit", "rail")) {
  print("Didn't I tell you not to try that? Sheesh!");
}

if (said("o", "ballantine", "d") || said("o", "ballantine") || said("o", "d") || said("o", "building", "d")) {
  if (posn(o0, 41, 94, 54, 109)) {
    program.control();
    load.sound(3);
    sound(3, f153);
    end.of.loop(o1, f101);
  } else {
    print("You're not close enough.");
  }
}

if (f101) {
  move.obj(o0, 53, 104, 1, f100);
}

if (f100) {
  reset(f101);
  ignore.blocks(o0);
  ignore.objs(o0);
  move.obj(o0, 44, 104, 1, f111);
}

if (f3) {
  erase(o1);
  reset(f100);
  reset(f101);
  reset(f107);
  reset(f112);
  reset(f111);
  reset(f118);
  new.room(125);
}

if (v2 == 2) {
  erase(o1);
  reset(f100);
  reset(f101);
  reset(f107);
  reset(f112);
  reset(f111);
  reset(f118);
  new.room(127);
}
return();


// messages
#message 1 "You're in front of Ballantine Hall."
#message 2 "Yup. Ballantine, like the beer."
#message 3 "You see nothing important."
#message 4 "Na' ga' do it!."
#message 5 "Yes, there is a tree here."
#message 6 "It leads to the door."
#message 7 "You see nothing."
#message 8 "Students usually sit on it. Now that doesn't mean you should try it."
#message 9 "Didn't I tell you not to try that? Sheesh!"
#message 50 "You're not close enough."
