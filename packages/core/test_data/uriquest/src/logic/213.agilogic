if (f5) {
  load.pic(v0);
  draw.pic(v0);
  discard.pic(v0);
  set.horizon(0);

  if (v1 == 212) {
    position(o0, 27, 122);
    v6 = 3;
  }

  if (v1 == 214) {
    position(o0, 120, 122);
    v6 = 7;
  }
  reset(f108);
  reset(f117);
  reset(f109);
  reset(f110);

  if (!f164) {
    animate.obj(o1);
    load.view(64);
    set.view(o1, 64);
    set.loop(o1, 1);
    set.cel(o1, 0);
    position(o1, 80, 122);
    start.cycling(o1);
    draw(o1);
    load.sound(10);
    set(f110);
  }
  draw(o0);
  show.pic();
}
get.posn(o0, v100, v101);
v104 = v100;
v106 = v100;
v105 = v101;
v107 = v101;
v104 -= 15;v105 -= 10;v106 += 20;v107 += 10;get.posn(o1, v102, v103);

if (((v102 > v104)) && ((v102 < v106)) && ((v103 > v105)) && ((v103 < v107))) {
  set(f108);
} else {
  reset(f108);
}

if (!f164) {
  wander(o1);

  if (f110) {
    sound(10, f110);
  }
}

Address166:

if (!f164) {
  if (said("give", "condom") || said("give", "condom", "bum")) {
    if (f108) {
      drop(i1);
      set(f164);
      v3 += 1;
      load.sound(1);
      sound(1, f153);
      print("\"DUDE!!!\" says the crazy naked guy. \"You are my hero!  I was just about to get laid, when all of a sudden, I realize that I have no condom!! My girlfriend is really picky about that!! Thank you so much, now I have to get back to the room and get some serious booty!\"");
      v3 += 1;
      load.sound(12);
      sound(12, f153);
      print("\"It's not much, but take this cash. Use it to get yourself some booty! Bye!\" says the crazy naked guy.");
      get(i8);
      set(f109);
    } else {
      print("Try getting closer.");
    }
  }
}

Address232:

if (f109) {
  move.obj(o1, 130, 122, 1, f117);
}

if (f117) {
  reset(f109);
  reset(f117);
  erase(o1);
}

if (said("examine") || said("examine", "a") || said("examine", "room")) {
  print("This is a hallway inside of Bressler Hall.");
}

if (said("examine", "hall")) {
  print("It's concrete, dirty, and ugly.");
}

if (said("examine", "d")) {
  print("The doors lead to rooms.");
}

if (said("examine", "bum")) {
  if (!f164) {
    print("There's a crazy naked guy running around.");
  } else {
    print("Huh?");
  }
}

if (said("chat", "bum")) {
  if (!f164) {
    print("He seems to be ignoring you.");
  } else {
    print("Huh?");
  }
}

if (said("o", "d") || said("knock", "d")) {
  print("Nah, you don't know any of these people.");
}

if (said("examine", "wall") || said("examine", "floor") || said("examine", "ceiling")) {
  print("They're pretty much the same as the rest of the dismal dorm.");
}

if (said("chat")) {
  print("You should stop talking to yourself.");
}

if ((f3) && (posn(o0, 1, 60, 60, 150))) {
  reset(f108);
  reset(f117);
  reset(f109);
  reset(f110);
  new.room(212);
}

if ((f3) && (posn(o0, 61, 60, 159, 150))) {
  reset(f108);
  reset(f117);
  reset(f109);
  reset(f110);
  new.room(214);
}
return();


// messages
#message 1 "\"DUDE!!!\" says the crazy naked guy. \"You are my hero!  I was just about to get laid, when all of a sudden, I realize that I have no condom!! My girlfriend is really picky about that!! Thank you so much, now I have to get back to the room and get some serious booty!\""
#message 2 "\"It's not much, but take this cash. Use it to get yourself some booty! Bye!\" says the crazy naked guy."
#message 3 "Try getting closer."
#message 4 "This is a hallway inside of Bressler Hall."
#message 5 "It's concrete, dirty, and ugly."
#message 6 "The doors lead to rooms."
#message 7 "There's a crazy naked guy running around."
#message 8 "Huh?"
#message 9 "He seems to be ignoring you."
#message 10 "Nah, you don't know any of these people."
#message 11 "They're pretty much the same as the rest of the dismal dorm."
#message 12 "You should stop talking to yourself."
#message 50 "You're not close enough."
