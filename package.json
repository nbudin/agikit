{
  "name": "root",
  "private": true,
  "devDependencies": {
    "husky": "^6.0.0",
    "lint-staged": "^10.5.4",
    "prettier": "^2.5.1",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{js,css,md}": "yarn run prettier --write"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
